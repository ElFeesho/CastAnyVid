apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'junit:junit:4.11'
}

sourceSets {
    test {
        java {
            srcDir("src/main/test")
        }
    }
}